{% extends "layout.html.twig" %}

{% set level = entity %}

{% block detail %}
    <ul class="record_actions">
        {{ form(edit_form) }}
    </ul>
    <p><a href="{{ path('app_level_show', { 'id': level.id }) }}"> Return to {{ level }}</a></p>
{% endblock detail %}

{% block sidebar %}
    <h3><a href="{{ path('app_level') }}">View All Levels</a></h3>
{% endblock sidebar %}

