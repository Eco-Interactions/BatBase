{% extends "layout.html.twig" %}

{% set attribution = entity %}

{% block detail %}
    <ul class="record_actions">
        {{ form(edit_form) }}
</ul>
    <p><a href="{{ path('app_attribution_show', { 'id': attribution.id }) }}"> Return to {{ attribution }}</a></p>
{% endblock detail %}

{% block sidebar %}
    <h3><a href="{{ path('app_attribution') }}">View All Attributions</a></h3>
{% endblock sidebar %}

